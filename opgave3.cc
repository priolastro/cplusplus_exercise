#include<iostream>
#include<vector>
#include<string.h>


#define _USE_MATH_DEFINES
using namespace std;


bool sumOfTwoEqualsThird(int arr[], int length){
    cout << arr[0] << endl;
    cout << length << endl;

    for ()
    return 0;
}

int main() {
    int first[5] = {17, 29, 512, 58, 43};
    int size_first = sizeof(first)/sizeof(first[0]);
    cout << sumOfTwoEqualsThird(first, size_first) << endl;
}